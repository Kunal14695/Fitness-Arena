# Local Development (SQLite)
DATABASE_URL="file:./dev.db"

# Production (PostgreSQL on Neon / Supabase / Render)
# DATABASE_URL="postgresql://user:password@host:5432/fitness_arena?sslmode=require"

PORT=5000
JWT_SECRET="fitness-arena-super-secret-jwt-key-2026"
NODE_ENV="production"
